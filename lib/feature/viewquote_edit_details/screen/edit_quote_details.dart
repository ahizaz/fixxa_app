import 'package:flutter/material.dart';

class EditQuoteDetails extends StatelessWidget {
  const EditQuoteDetails({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      
    );
  }
}